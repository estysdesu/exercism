module Bob exposing (hey)
-- parser?
import Parser exposing (Parser)


hey : String -> String
hey remark =
    if remark == String.toUpper remark then
        "Whoa, chill out!"
    else if

    else
        "Fine. Be that way!"
